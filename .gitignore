# dependencies / build output
node_modules/
dist/
dist-electron/
release/
coverage/

# package-manager caches
.npm-cache/
.npm-logs/
.vite/
.electron-cache/
.parcel-cache/
.eslintcache/
.cache/

# runtime data (smoke tests write userData here)
.smoke-data/
app-data/

# local secret store — never commit (production data lives in %APPDATA%)
secrets.json
secrets.json.key
hosts.json
providers.json
app-settings.json
codex-app-server.json
*.credential
*.secret

# environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12

# logs / diagnostics
*.log
pnpm-debug.log
yarn-debug.log
yarn-error.log
*.tsbuildinfo

# local handoff prompt — never commit or push
docs/AI_HANDOFF_PROMPT.md

# AI-internal handoff / progress docs — never commit or push
docs/AI_PROJECT_GUIDE.md
docs/agent-file-tools.md
docs/progress/

# user-local scripts — never commit or push
enable-ssh.ps1

# editor / OS noise
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db
*.swp
*~
*.tmp
*.bak
*.orig
*.rej
